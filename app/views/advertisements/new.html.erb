<h1>New Advertisement</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guideline for advertisements</p>
    <ul>
      <li>Claims must be truthful, cannot be deceptive or unfair, and must be evidence-based.</li>
      <li>The incessant use of hashtags will get you banned.</li>
    </ul
  </div>

  <div class="col-md-8">

    <%= form_for @advertisement do |f| %>

      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter advertisement title" %>
      </div>
      <div class="form-group">
        <%= f.label :copy %>
        <%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter advertisement copy" %>
      </div>
      <div class="form-group">
        <%= f.label :price %>
      
        <div class="radio">  
            <%= f.radio_button :price, '250', :checked => true %>
            <%= f.label :price, '$250', :value => '250' %>
          </div>
          <div class="radio">
            <%= f.radio_button :price, '200' %>
            <%= f.label :price, '$200', :value => '200' %>
          </div>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>

